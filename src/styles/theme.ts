import { css } from 'styled-components';

const font = css`
  font-style: normal;
  font-weight: 400;
  font-size: 18px;
  line-height: 22px;
  font-family: 'Inter';
`;

export const theme = { font };
