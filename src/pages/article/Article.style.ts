import { MentorSkills, ProfileBtn } from '@profile/Profile.style';
import styled from 'styled-components';

export const Profile = styled.div`
  display: flex;
  justify-content: space-between;
`;

export const ProfileDescription = styled.div``;

export const Name = styled(MentorSkills);

export const ActivityTime = styled(MentorSkills);

export const ProgressBtn = styled(ProfileBtn);

export const ArticleData = styled.section``;

export const Title = styled.h1``;
